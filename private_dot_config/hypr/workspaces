# This file is read by ./set-workspaces.sh and determines the list of workspaces and default applications.
# Lines starting with `#` are comments.
# Comments and blank lines will both be ignored.
# Each line has three parts separated by semicolons:
## BUTTON, this is the button that will be bound tho that workspace.
## NAME, this is the name of the workspace in waybar.
## COMMAND, (optional, if omitted, the semicolon must still be there) this is the default command to run on $mod+$alt+Space.
## ALTCOMMAND, (optional), this is the secondary command to run on $mod+$alt+Shift+Space


# Entertainment:
F1;     ; steam;
F2;     󰈹; firefox;
F3;     ; discord;
F4;     ; spotify;

# Development:
F5;     ; code;        foot neovim
F6;     ; foot lazygit;
F7;     󰂫; blender;
F8;     ; godot;

# System:
F9;     ; foot;
F10;    ; foot yazi;   nemo
F11;    ; foot btop;
F12;    ; 

# Office:
Print;  ; 
# Why is this button name so fucking long.
Scroll_Lock; ;    libreoffice;
Pause;  ; thunderbird;
